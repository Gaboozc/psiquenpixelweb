export default function Merch() {
  return (
    <section className="min-h-screen flex items-center justify-center">
      <p
        className="text-brand-muted text-sm tracking-widest"
        style={{ fontFamily: 'var(--font-pixel)' }}
      >
        ▓ PRÓXIMAMENTE ▓
      </p>
    </section>
  );
}
